apiVersion: v1
kind: Pod
metadata:
# labels can add extra information to the PODS and used as internal resource selectors
  name: labels
  labels:
    project: expense
    module: backend
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx